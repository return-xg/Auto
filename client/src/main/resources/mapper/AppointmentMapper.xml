<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auto.mapper.AppointmentMapper">

    <resultMap type="Appointment" id="AppointmentResult">
        <result property="id"    column="id"    />
        <result property="orderId"    column="order_id"    />
        <result property="storeId"    column="store_id"    />
        <result property="userId"    column="user_id"    />
        <result property="appointmentTime"    column="appointment_time"    />
        <result property="status"    column="status"    />
        <result property="note"    column="note"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectAppointmentVo">
        select id, order_id, store_id, user_id, appointment_time, status, note, create_time, update_time
        from appointment
    </sql>

    <select id="selectAppointmentList" parameterType="Appointment" resultMap="AppointmentResult">
        <include refid="selectAppointmentVo"/>
        <where>
            <if test="orderId != null">
                and order_id = #{orderId}
            </if>
            <if test="storeId != null">
                and store_id = #{storeId}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectAppointmentListByOrderId" parameterType="Long" resultMap="AppointmentResult">
        <include refid="selectAppointmentVo"/>
        where order_id = #{orderId}
        order by create_time desc
    </select>

    <select id="selectAppointmentListByUserId" parameterType="Long" resultMap="AppointmentResult">
        <include refid="selectAppointmentVo"/>
        where user_id = #{userId}
        order by create_time desc
    </select>

    <select id="selectAppointmentById" parameterType="Long" resultMap="AppointmentResult">
        <include refid="selectAppointmentVo"/>
        where id = #{id}
    </select>

    <insert id="insertAppointment" parameterType="Appointment" useGeneratedKeys="true" keyProperty="id">
        insert into appointment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderId != null">order_id,</if>
            <if test="storeId != null">store_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="appointmentTime != null">appointment_time,</if>
            <if test="status != null">status,</if>
            <if test="note != null and note != ''">note,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderId != null">#{orderId},</if>
            <if test="storeId != null">#{storeId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="appointmentTime != null">#{appointmentTime},</if>
            <if test="status != null">#{status},</if>
            <if test="note != null and note != ''">#{note},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateAppointment" parameterType="Appointment">
        update appointment
        <trim prefix="SET" suffixOverrides=",">
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="storeId != null">store_id = #{storeId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="appointmentTime != null">appointment_time = #{appointmentTime},</if>
            <if test="status != null">status = #{status},</if>
            <if test="note != null and note != ''">note = #{note},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAppointmentById" parameterType="Long">
        delete from appointment where id = #{id}
    </delete>

    <delete id="deleteAppointmentByIds" parameterType="String">
        delete from appointment where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteAppointmentByOrderId" parameterType="Long">
        delete from appointment where order_id = #{orderId}
    </delete>

</mapper>
